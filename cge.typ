= Simple CGE Modeling

Two countries: ($h,f$).

Two final goods: ($c^h, c^f$)

Energy production


= Social Accounting Matrix

Read from columns to rows.

Can be more or less disintegrated.

Leontieff: input-output matrix